{
  "name": "ayaya-jp",
  "version": "0.1.0",
  "private": true,
  "description": "日语练习",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:pages": "node scripts/build-pages.js",
    "check": "npm run validate && npm run validate:bundle && npm run test:contracts",
    "test": "npm run check && npm run test:browser",
    "test:browser": "playwright test",
    "test:contracts": "node scripts/test-app-contracts.js",
    "update:manifest": "node scripts/update-validation-manifest.js",
    "validate": "node scripts/validate.js",
    "validate:bundle": "node validate-bundle.js"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
